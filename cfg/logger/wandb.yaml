# cfg/logger/wandb.yaml
class_path: pytorch_lightning.loggers.WandbLogger
init_args:
  project: drumblender
  name: ${oc.env:RUN_NAME,modal_step}
  save_dir: logs/wandb
  log_model: true
dict_kwargs:
  job_type: train
